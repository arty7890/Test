# Reassembly notes

Use the repository mirror convention already present in this repository: base64 parts are joined in lexical order, decoded into the tar.xz archive, and then verified against the SHA-256 value in export_manifest.md.
